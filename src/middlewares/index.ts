import error from './error';
import login from './login.middleware';
import product from './product.middleware';
import user from './user.middleware';
import auth from './auth';
import order from './order.middleware';

export default { error, login, product, user, auth, order };